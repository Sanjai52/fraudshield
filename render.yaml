services:
  - type: web
    name: fraudshield
    runtime: python
    rootDir: services/ai
    buildCommand: pip install -r requirements-ai.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13